<div class="row g-4">
  <div class="col-lg-6 mx-auto">
    <div class="card shadow p-4">
      <div class="text-center mb-4">
        <div class="d-flex justify-content-center align-items-center gap-2 mb-2">
          <span class="badge rounded-circle bg-rs-primary">RS</span>
          <span class="fw-bold text-rs-primary fs-4">RightSeat</span>
        </div>
        <h1 class="h4 fw-bold mb-1">Welcome back</h1>
        <p class="text-secondary small mb-0">Log in to manage your training.</p>
      </div>

      <form method="POST" action="/login">
        <div class="mb-3">
          <label for="email" class="form-label">Email address</label>
          <input type="email" class="form-control" id="email" name="email" placeholder="you@example.com" required>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label d-flex justify-content-between">
            <span>Password</span>
            <a href="#" class="small link-primary">Forgot Password?</a>
          </label>
          <input type="password" class="form-control" id="password" name="password" required>
        </div>

        <div class="form-check mb-3">
          <input class="form-check-input" type="checkbox" id="remember">
          <label class="form-check-label" for="remember">Remember me</label>
        </div>

        <button type="submit" class="btn btn-rs w-100">Log in</button>

        <div class="text-center mt-3">
          <span class="text-secondary small">Don’t have an account?</span>
          <a href="/sign-up" class="small link-primary ms-1">Sign up</a>
        </div>
      </form>
    </div>
  </div>
</div>
